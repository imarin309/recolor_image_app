# recolor_image_app

- exe: `streamlit run src/app.py`
- lint: `ruff check .`
- format: `ruff format .`
- Environment construction
  - `python -m venv .venv`
  - `sorce .venv/bin/activate`
  - `poetry install`
  - `poetry shell`
